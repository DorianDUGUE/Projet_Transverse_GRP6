Le dossier AJAX:

il contient les codes de tests de l'API Ajax en php qui permet l'intéraction avec la base de données. Cette dernière permet aussi de voir le contenue de la base récupéré par le .php à travers un tableau

Le dossier Simu:

Lui contient la page web représentant la base sur une MAP. C'est à dire à travers un code JS et leaflet, on représente le résultat de l'API Ajax sous forme de rond...
C'est cette dernière qui constitue le véritable rendu final et qui sera consulté.